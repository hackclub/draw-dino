services:
  app:
    build:
      context: .
      args:
        NEXT_PUBLIC_GITHUB_CLIENT_ID: ${NEXT_PUBLIC_GITHUB_CLIENT_ID}
        NEXT_PUBLIC_HACKCLUB_AUTH_URL: ${NEXT_PUBLIC_HACKCLUB_AUTH_URL}
    ports:
      - "80:80"
    env_file:
      - .env
